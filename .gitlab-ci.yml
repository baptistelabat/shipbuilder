stages:
    - build
    - deploy

build:
    stage: build
    script:
     - make
    artifacts:
      paths:
        - shipBuilder/index.html
        - shipBuilder/css
        - shipBuilder/assets
        - shipBuilder/js

pages:
  stage: deploy
  dependencies:
     - build
  script:
       - mv shipBuilder/ public/
  artifacts:
        when: on_success
        paths:
        - public
  only:
      - master
