module Update exposing (update)

import Model exposing (Model, Msg(..))


update : Msg -> Model -> Model
update msg model =
    case msg of
        NoOp ->
            model

        SelectPanel panel ->
            { model | panel = panel }
